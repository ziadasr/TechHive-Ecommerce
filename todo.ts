/**
 * !ecommerce app
 * //todo add product to website
 * //todo add product to db
 * //todo show products from db
 * //todo item details
 * //todo log in for admin
 * //todo admin dashboard
 * //todo products table in admin dashboard
 * //todo search in admin dashboard produts
 * //todo users model backend
 * //todo regestireation ui
 * //todo regestireation backend ==> (signup - login - verify - forget password)
 * //sepeate the addresses into a dingle table to allow more than one address for each user
 * !jwt
 * * todo adjust the logged in state to work with the token
 * //todo implement jwt tokens for users
 * //todo adjust the authentication to work with cookies
 * !user profile
 * //todo address model backend
 * //todo ui interface for profile page
 * todo profile page ==>
 * //view -
 * //add address -
 * //view addresses -
 * view orders
 * change password
 *
 * !cart
 * cart has a small error in rendering the backend responses could be skipped since there is no important data
 * //todo cart model backend
 * //todo addtocart
 * //todo get cart ==> backend dn
 * //todo remove one item FE-BE
 * //todo increment & decrement FE-BE
 * //todo clear cart ==> backend dn
 * !orders
 * //todo  review orders model backend
 * //todo add address
 * //todo orders methods add delete
 * !admin dashboard ===> phase 2
 * todo implement jwt tokens for admins
 * todo search in admin dashboard users
 * todo users table in admin dashboard
 * todo orders table in admin dashboard
 *
 ** **************************************
 * todo serializable transactions
 * todo deadlock
 * todo race conditions ==> code
 * todo double booking problem
 */
